import React from 'react'

export default function App() {
  // We don't really need App.jsx since we render AppRouter in main.jsx directly.
  // But keeping it if we want a global layout wrapper around everything.
  return null
}
